{
    "dependencies": {
        "@project-serum/anchor": "^0.16.1",
        "express": "^4.17.1"
    },
    "devDependencies": {
        "chai": "^4.3.4",
        "mocha": "^9.0.3"
    },
    "scripts": {
        "build": "cd app && npm install && npm run build"
      }
}
